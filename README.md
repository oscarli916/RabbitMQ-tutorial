# A simple example using RabbitMQ in Python (pika library)

## RabbitMQ

Use docker image to spin up RabbitMQ defined in `docker-compose.yml` file

`docker compose up`

## Consumer

`app/src/consume.py`

## Publisher

`app/src/publish.py`
